package org.codefeedr.keymanager.zookeeper

import org.codefeedr.keymanager.KeyManager

class ZookeeperKeyManager extends KeyManager {
  override def request(target: String, numberOfCalls: Int): String = ???
}
